// Criar uma função que recebe dois parâmetros e retorna a potencia entre eles

function potencia(num, expoente) {

    let resultado = Math.pow(num, expoente)

    return console.log(resultado)
}

potencia(5, 5)
potencia(2,3)
potencia(3,2)
potencia(10, 2)